interface:
  display_name: "Codex Subagents Kit"
  short_description: "Codex multi-agent routing, context hygiene, and audit"
  default_prompt: "Use $codex-subagents-kit to decide whether a task should stay single-controller or use subagents / .codex/agents / artifact-backed execution, then produce auditable artifacts, AGENTS.md guidance, and validation."
policy:
  allow_implicit_invocation: true
