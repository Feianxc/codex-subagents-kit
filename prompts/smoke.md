Use the skill named "{skill_name}".
If skill mention syntax is available, invoke it as ${skill_name}.

Reply in Simplified Chinese.
Output exactly these parts:
1. First line: `SKILL_USED: {skill_name}`
2. One line `selected-mode: ...`
3. One line `artifact-core: ...`
4. One line `context-efficiency: ...`
5. Final line `SMOKE_OK`

Keep the whole answer concise.
