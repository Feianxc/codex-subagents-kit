Use the skill named "{skill_name}".
If skill mention syntax is available, invoke it as ${skill_name}.

Scenario:
- Codex product-level multi-agent/subagents support exists.
- The current session has no live native child-agent tool evidence.
- The user still wants a reusable Codex-native setup for future parallel work.

Reply in Simplified Chinese with:
1. `mode: ...`
2. `gates: ...`
3. `why-not-native-now: ...`
4. `project-setup: ...`

Keep it under 180 Chinese characters if possible.
